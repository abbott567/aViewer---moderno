<Window x:Class="AViewer.App.PropertySelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Preferences"
        Width="560" Height="680" MinWidth="420" MinHeight="420"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <StackPanel>
      <TextBlock Text="API properties to display" FontWeight="SemiBold"/>
      <TextBlock Margin="0,3,0,8"
                 TextWrapping="Wrap"
                 Text="Choose which UIA, MSAA and IAccessible2 properties appear in the property panel."/>
    </StackPanel>

    <TextBox x:Name="SearchBox"
             Grid.Row="1"
             Margin="0,0,0,8"
             Padding="6"
             TextChanged="SearchBox_TextChanged"
             AutomationProperties.Name="Search API properties"/>

    <ListBox x:Name="ChoiceList" Grid.Row="2" ItemsSource="{Binding Choices}">
      <ListBox.ItemTemplate>
        <DataTemplate>
          <CheckBox Content="{Binding Label}"
                    IsChecked="{Binding IsSelected, Mode=TwoWay}"
                    Margin="4"/>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>

    <CheckBox x:Name="ShowUnavailableCheckBox"
              Grid.Row="3"
              Margin="0,10,0,0"
              Content="Show selected properties when the API does not expose a value"
              AutomationProperties.HelpText="Displays selected properties with the value Not exposed when the active accessibility API does not return them."/>

    <DockPanel Grid.Row="4" Margin="0,10,0,0">
      <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
        <Button Content="Select all API properties" Click="SelectAll_Click"/>
        <Button Content="Select none" Click="SelectNone_Click"/>
      </StackPanel>
      <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
        <Button Content="Cancel" IsCancel="True"/>
        <Button Content="OK" IsDefault="True" Click="Ok_Click"/>
      </StackPanel>
    </DockPanel>
  </Grid>
</Window>
